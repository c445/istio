{{ if and .Values.global.certificate .Values.global.key }}
apiVersion: v1
kind: Secret
type: kubernetes.io/tls
metadata:
  name: istio-ingressgateway-certs
data:
  tls.crt: {{ .Values.global.certificate | b64enc }}
  tls.key: {{ .Values.global.key | b64enc }}
{{ end }}
